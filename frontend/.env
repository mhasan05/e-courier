# Copy to .env.local to point the frontend at the Django API.
# Leave unset to keep using the in-memory mock stores.
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_API_URL=https://hospital-producers-ann-guam.trycloudflare.com/api/v1
